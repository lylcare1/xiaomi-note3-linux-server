# Override for systemd-time-wait-sync.service
# 默认 TimeoutStartSec=infinity, 没有网络时间源时会无限 hang, 阻塞 sysinit.target
# 导致 sshd (multi-user.target) 永不启动.
# 设置 30 秒超时, 让 sysinit.target 能继续推进.
[Service]
TimeoutStartSec=30
